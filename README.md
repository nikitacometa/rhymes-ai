# RhymePadre 🎤💋

> *Твоя милашка-реперша, которая шарит за рифмы~*

Telegram-бот для поиска сложных рифм по фонетике. Мультисиллабические, внутренние, слант-рифмы — всё это умеет RhymePadre.

## Что умеет

- 🔮 **Поиск рифм** — ищет по звучанию, не только по буквам
- ✨ **AI-генерация** — придумывает новые рифмы через OpenAI
- 🎭 **Сравнение фраз** — показывает % сходства и тип рифмы
- 📎 **Импорт текстов** — загружает .txt/.md файлы и разбирает на рифмы
- 📊 **Статистика** — сколько рифм собрано в базе

## Запуск

```bash
# 1. Настройка
cp .env.production.example .env
nano .env  # TELEGRAM_BOT_TOKEN обязателен, OPENAI_API_KEY для AI

# 2. Запуск
docker-compose up -d --build

# 3. Логи
docker-compose logs -f app
```

## Telegram-бот

### Меню кнопок

| Кнопка | Действие |
|--------|----------|
| 🔮 Найти в базе | Поиск рифм в коллекции |
| ✨ AI рифмы | LLM генерация новых рифм |
| 🔥 Найти все рифмы | База + AI вместе |
| 📚 Все рифмы | Показать всю коллекцию |
| 🔍 Анализ текста | Найти рифмы в файле (без сохранения) |
| 🎭 Сравнить | Проверить, рифмуются ли фразы |
| 📊 Статистика | Статистика базы |
| 💝 Помощь | Справка |

### Команды (олдскул)

| Команда | Пример |
|---------|--------|
| `/start` | Запуск бота |
| `/search фраза` | `/search оскала` |
| `/ai фраза` | `/ai вин дизель` |
| `/full фраза` | `/full панч` |
| `/compare фраза1 \| фраза2` | `/compare пол-оскала \| Ла Скала` |
| `/stats` | Статистика |

### Быстрый поиск

Просто напиши слово — бот поймёт и поищет в базе.

## Формат файла для импорта

```markdown
## Название трека
[Куплет]
Строка раз
Строка два

[Припев]
Строка три
```

## Архитектура

```
src/
├── modules/
│   ├── telegram/     # Telegram бот (Telegraf)
│   ├── rhyme/        # Сервис рифм + LLM
│   ├── phonetic/     # Фонетический анализ
│   ├── parser/       # Парсинг текстов
│   └── prisma/       # ORM
```

## Стек

- **Backend:** NestJS + TypeScript
- **БД:** PostgreSQL + Prisma
- **Кэш:** Redis
- **Bot:** Telegraf
- **AI:** OpenAI API

## Локальная разработка

```bash
npm install
cp .env.example .env
npm run start:dev
```

PostgreSQL и Redis должны быть запущены локально.

## Env-переменные

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | ✅ |
| `OPENAI_API_KEY` | API ключ OpenAI | ❌ (для AI) |
| `DATABASE_URL` | PostgreSQL URL | ✅ |
| `REDIS_URL` | Redis URL | ✅ |
| `BOT_NAME` | Имя бота в интерфейсе | ❌ (дефолт: Рифмоплётка) |

---

*Made with 💋 by humans and AI*
