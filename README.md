# RhymePadre 🎤

Telegram-бот для поиска сложных рифм по фонетике.

## Запуск

```bash
# 1. Настройка
cp .env.production.example .env
nano .env  # вставить TELEGRAM_BOT_TOKEN

# 2. Запуск
docker-compose up -d --build

# 3. Логи
docker-compose logs -f app
```

## Telegram-бот

| Команда | Пример |
|---------|--------|
| Поиск | `/search оскала` или просто `оскала` |
| Сравнение | `/compare пол-оскала \| Ла Скала` |
| Статистика | `/stats` |
| Импорт | отправить `.txt` или `.md` файл |

## Формат файла для импорта

```markdown
## Название трека
[Куплет]
Строка раз
Строка два

[Припев]
Строка три
```

## Локальная разработка

```bash
npm install
cp .env.example .env
npm run start:dev
```

PostgreSQL и Redis должны быть запущены локально.
