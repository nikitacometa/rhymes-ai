# RhymePadre 🎤

Telegram-бот для хранения и поиска сложных рифм (мультисиллабические, внутренние, слант-рифмы).

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env, добавив TELEGRAM_BOT_TOKEN и OPENAI_API_KEY
```

### 3. Запуск базы данных

```bash
npm run docker:up
```

### 4. Применение миграций

```bash
npm run prisma:migrate
```

### 5. Запуск приложения

```bash
npm run start:dev
```

## Технологии

- **Backend**: NestJS + TypeScript
- **Database**: PostgreSQL + Prisma ORM
- **Queue**: Redis + BullMQ
- **Bot**: Telegraf
- **AI**: OpenAI API (для фонетизации сложных случаев)

## Структура проекта

```
src/
├── main.ts                 # Точка входа
├── app.module.ts           # Корневой модуль
└── modules/
    ├── prisma/             # Работа с БД
    ├── rhyme/              # Домен рифм (families, units, links)
    ├── phonetic/           # Фонетизация текста
    ├── parser/             # Парсинг текстов и извлечение рифм
    └── telegram/           # Telegram-бот
```

## Команды

| Команда | Описание |
|---------|----------|
| `npm run start:dev` | Запуск в режиме разработки |
| `npm run build` | Сборка проекта |
| `npm run prisma:studio` | Открыть Prisma Studio (GUI для БД) |
| `npm run docker:up` | Запустить PostgreSQL и Redis |
| `npm run docker:down` | Остановить контейнеры |

