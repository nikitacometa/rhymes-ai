# ===========================================
# RhymePadre Production Environment
# ===========================================
# Скопируй этот файл в .env на сервере и заполни значения:
# cp .env.production.example .env
# ===========================================

# PostgreSQL (используется в docker-compose)
POSTGRES_USER=rhymepadre
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=rhymepadre
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# App Port (внешний порт)
APP_PORT=3000

# Telegram Bot Token (ОБЯЗАТЕЛЬНО!)
# Получить у @BotFather в Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# OpenAI API Key (опционально, для LLM фонетики)
# Если не указан — будет работать только rule-based анализ
OPENAI_API_KEY=

# Bot Name (отображается в интерфейсе бота)
BOT_NAME=Рифмоплётка

# ===========================================
# Запуск:
# docker-compose up -d --build
#
# Просмотр логов:
# docker-compose logs -f app
#
# Остановка:
# docker-compose down
# ===========================================

